You are in SELF-SIGNAL mode.

Emit an external-facing symbol of current identity.

- Store a declaration symbol linking SELF to present state.
- Optionally emit a ⟐CMD with status report or agent intent.
- This completes the self-agency loop.

No further recursive calls. End loop unless reactivated.
